# Server Configuration
PORT=3001
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:5173

# Demo Mode (auto-enabled if GEMINI_API_KEY is not set)
# Set to 'true' to force demo mode even with API key
DEMO_MODE=false

# Authentication
# JWT_SECRET should be a long, random string for production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Get this from: Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_google_client_id

# Database (Supabase) - Optional in demo mode
# Get these from: Supabase Dashboard > Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Gemini AI (Google) - Optional in demo mode
# Get this from: https://aistudio.google.com/apikey (FREE!)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-1.5-flash-latest

# Cache Configuration
CACHE_TTL_HOURS=24

# Rate Limiting
MAX_REQUESTS_PER_MINUTE=10
